For all standard vector types, bitcast and convert to self

don't forget to write wrappers for sin, cos, log, exp etc for shim classes

use std::fma on neon, but not on sse2 (as it's not hardware supported and too slow)

refactor everything into f32x4 etc
